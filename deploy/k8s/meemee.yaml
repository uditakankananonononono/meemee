apiVersion: v1
kind: Namespace
metadata:
  name: meemee
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: meemee-data
  namespace: meemee
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests: {storage: 5Gi}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api
  namespace: meemee
spec:
  replicas: 1
  selector: {matchLabels: {app: meemee-api}}
  template:
    metadata: {labels: {app: meemee-api}}
    spec:
      securityContext: {runAsNonRoot: true, seccompProfile: {type: RuntimeDefault}}
      containers:
        - name: api
          image: ghcr.io/uditakankananonononono/meemee:0.83.0
          args: ["meemee", "serve", "--host", "0.0.0.0", "--port", "8787"]
          ports: [{containerPort: 8787}]
          envFrom: [{secretRef: {name: meemee-secrets}}]
          volumeMounts: [{name: data, mountPath: /home/meemee/.meemee}]
          securityContext: {allowPrivilegeEscalation: false, capabilities: {drop: ["ALL"]}}
          readinessProbe: {httpGet: {path: /health, port: 8787}, initialDelaySeconds: 3}
          resources:
            requests: {cpu: 100m, memory: 128Mi}
            limits: {cpu: "1", memory: 1Gi}
      volumes: [{name: data, persistentVolumeClaim: {claimName: meemee-data}}]
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: worker
  namespace: meemee
spec:
  replicas: 2
  selector: {matchLabels: {app: meemee-worker}}
  template:
    metadata: {labels: {app: meemee-worker}}
    spec:
      securityContext: {runAsNonRoot: true, seccompProfile: {type: RuntimeDefault}}
      containers:
        - name: worker
          image: ghcr.io/uditakankananononono/meemee:0.83.0
          args: ["meemee", "worker"]
          envFrom: [{secretRef: {name: meemee-secrets}}]
          volumeMounts: [{name: data, mountPath: /home/meemee/.meemee}]
          securityContext: {allowPrivilegeEscalation: false, capabilities: {drop: ["ALL"]}}
          resources:
            requests: {cpu: 100m, memory: 128Mi}
            limits: {cpu: "1", memory: 1Gi}
      volumes: [{name: data, persistentVolumeClaim: {claimName: meemee-data}}]
---
apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: meemee
spec:
  selector: {app: meemee-api}
  ports: [{port: 80, targetPort: 8787}]
