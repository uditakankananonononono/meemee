[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "meemee-client"
version = "0.122.0"
description = "Typed Python client SDK for the Meemee agent platform API (targets server v0.122.0)."
readme = "README.md"
requires-python = ">=3.10"
license = "LicenseRef-Proprietary"
authors = [{ name = "Udita Phookan" }]
license-files = ["LICENSE"]
dependencies = [
    "httpx>=0.27,<1.0",
    "pydantic>=2.5,<3.0",
    "typing-extensions>=4.7,<5",
]

[project.optional-dependencies]
ws = ["websockets>=13,<16"]
dev = ["pytest>=8.0", "pytest-asyncio>=0.23,<1", "websockets>=13,<16"]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
