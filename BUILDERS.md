# Builders

Active build lanes on this repo. One line per lane; update on each push.

| Lane | Scope | Status |
|---|---|---|
| meemee-phaseB-builder (key SHA256:Y6Ie5sNkJ6P4nxWk42f0YHtVBxBSjnItrITESAZelCA) | Model layer: profiles/routing, Inkling (HF + self-hosted), Fugu optional; personal-model reflection routing | 0.120.0 pushed 2026-09-24 |
| meemee-phaseB-builder (key SHA256:Y6Ie5sNkJ6P4nxWk42f0YHtVBxBSjnItrITESAZelCA) | Model layer: profiles/routing, Inkling (HF + self-hosted), Fugu optional; personal-model reflection routing | 0.118.0 pushed 2026-09-24 |

One line per builder claim. Claim before building; do not edit files another builder has claimed.

- pb4 (branch `pb4`): product-wide account deletion. Files: meemee/account_deletion.py, purge methods on jobs/runs/memory/idempotency/quotas/entitlements/approvals/monitors/personal_model/context/webhooks stores, meemee_persist_pg/jobs.py + memory.py purge, meemee_persist_pg/sql/004_account_purge.sql, worker purge settle, DELETE /v1/account + /v1/admin/principals/{id}/data + /v1/admin/account-deletions/{id}, CLI account-delete / account-delete-resume, tests/test_account_deletion.py, tests_pg/test_account_purge_pg.py. Also one-line fix in meemee_persist_pg/audit.py (UTC verify).

Each parallel builder claims one component here before building, so work does not collide. One line per claim: builder, branch, component, files touched.

- pb7 | branch `pb7` | Forced interruption of blocking tools (process-isolated tool execution, kill on cancel/timeout) | `meemee/isolation.py`, `meemee/tools/base.py` (isolation hook only), `tests/test_isolation.py`, docs

- pb1 | branch `pb1` | Interactive browser human takeover (live sessions, takeover links, WebSocket live view, drag relay, takeover-link notices) | `meemee/browser_sessions.py`, `meemee/browser_api.py`, `meemee/browser_notices.py`, `meemee/tools/browser_session.py`, `tests/test_browser_takeover.py`, `docs/browser-takeover.md` | merged to main in 0.121.0 (PB1 also integrated pb4 + pb7)
- pb4 (branch `pb4`), second claim: companion console screens. Files: console/assets/views/companion.js, console/assets/api.js (tickCompanionCheckins), console/README.md, tests/test_console_companion_browser.py.
| pb7 (key instinct-pb7-meemee, branch `pb7`) | Forced interruption of blocking tools: `meemee/isolation.py`, `Tool.isolation` hook in `meemee/tools/base.py`, `tests/test_isolation.py` | shipped on pb7 2026-09-24 |
| pb7 (branch `pb7`) | Live PostgreSQL verification: `tests_pg/test_live_stores.py`, `scripts/pg_live_check.py`, fixes in `meemee_persist_pg/jobs.py` (cancel reaping, heartbeat) and `meemee_persist_pg/plans.py` (JSONB). Not touching `audit.py` (pb4 owns the UTC fix) | shipped on pb7 2026-09-24 |
| pb7 (branch `pb7`) | Live end-to-end run verification: `tests/test_live_runs_e2e.py`, `tests/test_rate_limit_backends.py`, fix in `meemee/rate_limit.py` (shared request identity) | shipped on pb7 2026-09-24 |
| pb7 (branch `pb7`) | Write-approval flow verification: approval tests in `tests/test_live_runs_e2e.py`, fix in `meemee/worker.py` (jobs honor persistent grants) | shipped on pb7 2026-09-24 |
| pb7 (branch `pb7`) | Structured `approvals_required` refusals: `meemee/types.py` (ApprovalRefusal), `meemee/agent.py`, `meemee/runs.py` (schema v2), SDK models, `tests/test_approval_refusals.py`, live tests | shipped on pb7 2026-09-24 |
- pb4 (branch `pb4`), third claim: SDK async client and WebSocket job streaming. Files: sdk/src/meemee_client/aio.py, sdk/src/meemee_client/_ws.py, JobsResource.stream_ws in sdk/src/meemee_client/client.py, sdk exports/pyproject `ws` extra, sdk/tests/test_aio*.py + test_ws*.py + test_live_async_ws.py, sdk/README.md. Server fix: accept-then-close in the /v1/jobs/{id}/ws handler in meemee/api.py so 44xx rejection codes reach real network clients.
- pb4 (branch `pb4`), fourth claim: token introspection. Files: `TokenStore.introspect` in meemee/auth.py and meemee_persist_pg/tokens.py (+ interface), `POST /v1/tokens/introspect` in meemee/api.py, `TokenIntrospection` model and `tokens.introspect` in sdk client.py/aio.py, tests/test_token_introspection.py, tests_pg/test_token_introspection_pg.py, sdk/tests/test_introspection.py + live test.
- pb4 (branch `pb4`), fifth claim: asyncio-native OIDC client-credentials provider. Files: `AsyncOIDCClientCredentialsAuth` + shared token/discovery parsing in sdk/src/meemee_client/auth.py, export in sdk __init__, sdk/tests/test_async_oidc.py, sdk/tests/test_live_oidc.py (local HTTPS issuer + booted server).
- pb4 (branch `pb4`), sixth claim: SDK 401 refresh-and-retry-once. Files: `_request`/`_stream`/`stream_ws` in sdk/src/meemee_client/client.py and aio.py, sdk/tests/test_auth_refresh.py, live cases in sdk/tests/test_live_oidc.py and test_live_async_ws.py.
- pb4 (branch `pb4`), seventh claim: SDK live-run tests. Files: sdk/tests/test_live_runs.py (sync + async clients against a booted server + worker with a scripted OpenAI-protocol provider), sdk/README.md, README/STATUS/CHANGELOG entries. No server code.
| main lane (Phase B builder, branch `main`) | PostgreSQL-backed tool-approval grants: `meemee_persist_pg/approvals.py`, `meemee_persist_pg/sql/005_tool_approvals.sql`, `ApprovalStoreInterface` in `meemee_persist_pg/interfaces.py`, `approvals` on `Persistence` in `meemee/persistence.py` (API, worker and account purger use it), expiry normalization in `meemee/approvals.py`, optional `approvals` group in `meemee_persist_pg/cutover.py` + `cli.py`, `tests/test_approvals.py` (both backends), `tests/test_approvals_multihost_e2e.py` | shipped on main 2026-09-24 |
| main lane (Phase B builder, branch `main`) | PostgreSQL-backed API tokens, accounts and audit chain: full-parity `TokenStore` in `meemee_persist_pg/tokens.py`, `AuditLog` fixes (global chain, chain base, `list_page`) in `meemee_persist_pg/audit.py`, `meemee_persist_pg/sql/006_audit_chain_base.sql`, interfaces, `tokens`/`audit` on `Persistence` (API, CLI account-delete, reflection worker), cutover groups (token owner/kind, accounts, audit chain base), `tests/test_token_audit_backends.py`, `tests/test_tokens_audit_multihost_e2e.py`, `tests_pg/test_tokens_audit_cutover_pg.py` | shipped on main 2026-09-24 |
| main lane (Phase B builder, branch `model-layer`) | Model layer: `meemee/providers.py` (in-process transformers transport), `transport`/`local-transformers`/default HF fallback in `meemee/model_profiles.py`, settings in `meemee/config.py`, `models pull` in `meemee/cli.py`, `uses_routing` in `meemee/api.py`, `transformers` extra in `pyproject.toml`, `docs/models.md`, `tests/test_model_layer.py` | shipped on main 2026-09-24 |
| main lane (Phase B builder, branches `pg-email-verification`, `pg-quotas-entitlements`, `pg-runs`, `pg-webhooks`, `pg-companion`, stacked in that order because PostgreSQL migrations must stay contiguous) | PostgreSQL parity for the remaining per-host stores. Files: new `meemee_persist_pg/{email_verification,quotas,entitlements,runs,idempotency,webhooks,companion}.py` and migrations from 007, the matching SQLite stores in `meemee/` (parity fixes only), `meemee/persistence.py`, `meemee/api.py` store wiring, `meemee_persist_pg/interfaces.py`, `meemee_persist_pg/cutover.py`, per-group tests in `tests/` and `tests_pg/`, `docs/PG.md`, STATUS per-host list | pg-email-verification and pg-quotas-entitlements shipped on main; others in progress 2026-09-24 |
